/**
 * Initialise l'application Calculator Synthwave.
 */
document.addEventListener('DOMContentLoaded', () => {
  // Les instances de Calculator, History et Theme sont déjà initialisées dans leurs fichiers respectifs.
  // On peut ajouter ici d'autres initialisations si besoin.
})
